#include "rect_generator.hpp"

namespace RP {
    RectGenerator::RectGenerator() : nameGenerator(NameGeneratorIOC::getInstance().constructNameGenerator()) {}
    Rectangle<int> RectGenerator::getUniquelyNamedRectangle(const std::vector<std::string>& takenNames) {
        while (true) {
            P
            Rectangle rectangle;
        }
    }
}
